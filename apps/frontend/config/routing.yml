# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/10-Routing

# default rules

categoryxml:
  url:  /map/categoryes.:sf_format
  class: sfRequestRoute
  param: { module: map, action: categoryes }
  requirements:
    sf_format: (?:xml)

placexml:
  url:  /map/places.:sf_format
  class: sfRequestRoute
  param: { module: map, action: places }
  requirements:
    sf_format: (?:xml)
    
place:
  url:  /map/save
  class: sfRequestRoute
  param: { module: map, action: save }
  requirements:
    sf_method: [post]

homepage:
  url:   /
  param: { module: map, action: index }


# generic rules
# please, remove them by adding more specific rules
#default_index:
#  url:   /:module
#  param: { action: index }

#default:
#  url:   /:module/:action/*
